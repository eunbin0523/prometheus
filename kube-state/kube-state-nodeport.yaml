apiVersion: v1
kind: Service
metadata:
  name: kube-state-nodeport
  namespace: kube-system
spec:
  type: NodePort
  selector:
    app: kube-state-metrics
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    nodePort: 30123
